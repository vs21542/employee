CREATE TABLE Employee
  (  
     id					int,
     name               varchar(255),
     sal                long,
     join_date          date,
     region             varchar(255),
     PRIMARY KEY(id)
  );